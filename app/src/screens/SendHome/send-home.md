# SendHome · SendHomeSuccess — заметка к React

Карты: `ds/screens/send-home.md`, `ds/screens/send-home-success.md`. Кадры: `369:329`, `372:380`.

**Из базы:** `AmountField` (новый, по мастеру `8:31`), `Button` (`ghost` / `secondary` / `primary` — `secondary` и `ghost` добавлены в базу под кит), `FeedRow`, `Toggle`, `Amount` (добавлен размер `display-lg` под `DS/Display/amount-lg`), `Orbit size="mini"` (новый режим того же компонента), `SliderConfirm` (новый, по мастеру `364:351`).

**Локально:** `Recipient` (аватар 72 + три строки + шеврон), `parts/StatusBar` — системная строка, общая для экранов действия; главный пока держит свою копию.

**Состояния семьи:** `plan` · `extra` · `insufficient` собраны пропсом; на канвасе первые два — скрытыми слоями, третье — скрытым полем, ссылкой и колонкой нехватки. `rate-expired`, `sending`, `failed` осознанно не собраны — `ia/open-questions.md` №116.

**Значения:** все из `ia/demo-data.md` §2, §6, §7, §7.1, §7.2; фикстура дополнена §7.2 (быстрые суммы, EGP, нехватка) и поправлена в §7.1: норма дня считается от рассвета — 301, а не 297.

**Оживление, которое здесь уже есть** (по договорённости с картой — это свойства самих компонентов, а не экрана): быстрые суммы меняют поле; тумблер пересчитывает остаток и снимает метку 25-го с мини-кольца; слайдер ведёт на успех; чип «ماما» на главном ведёт сюда; «تم» — на главный, «إرسال لشخص آخر» — обратно.

**Не собралось / внимание:** смена получателя (шеврон у аватара) — без поверхности; чипы Юсуфа и гамеи ведут на экран мамы; клавиатура для суммы не подключена — только быстрые суммы.
